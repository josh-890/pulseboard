# Dev database (default — safe for experimentation):
DATABASE_URL="postgresql://user:password@10.66.20.182:5432/pulseboard_dev"

# Production database is in .env.production (not committed).
# Copy .env.production.example to .env.production and fill in credentials.
